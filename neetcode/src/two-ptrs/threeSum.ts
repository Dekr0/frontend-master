function threeSum(nums: number[]): number[][] {
    // P: an integer array nums
    // R: an list of integer arrays, each array has a size of three
    //  the sum of each array is 0
    //  for each nums[i], nums[j], nums[k] in each array, i != j != k

    const triMap = new Set<string>();
    const twinPairs = new Map<number, number[]>();
    
    let i = 0, j = 0, k = 0;
    for ( ; i < nums.length; i++) {
        const r = 0 - nums[i];
        cosnt 
        if (!pairs.has(r)) pairs.set(r, []);
        for (j = 0; j < nums.length; j++) {
            if (j !== i) {
                
            }
        }
    }
}
