import { LNode } from './reversedLinkedList.ts';


function merge(headOne: LNode | null, headTwo: LNode | null): LNode | null {
    if (!headOne && !headTwo) return null;

    if (!headOne) return headTwo;

    if (!headTwo) return headOne;

    while (headOne || headTwo) {
        
    }

    return master;
}
